{
  "name": "recycle-railway-root",
  "version": "1.0.0",
  "type": "module",
  "main": "backend/api/server.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "cd backend/api && npm install && npm start",
    "build": "cd backend/api && npm install",
    "dev": "cd backend/api && npm run dev"
  },
  "dependencies": {},
  "description": "Railway deployment wrapper for recycle backend"
}